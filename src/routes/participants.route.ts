// import {
//   createParticipanst,
//   deleteParticipants,
//   updateParticipans,
// } from 'src/controllers/participants.controller';
// import { Router } from 'express';

// const router: Router = Router();

// router.post('/', createParticipanst);
// router.delete('/:id', deleteParticipants);
// router.put('/:conversationId', updateParticipans);

// export const participantsRoutes = router;
